
export const removeDuplicatedSeparators = (url: string): string =>
  url.replace(/(\w)\/{2,}/g, (match, p) => p + '/');

  (t = /url\([\'\"](.+)[\'\"]\)/gi.exec(e.backgroundImage))

const param = {
  parse(
    url?: string,
    options: IParamParseOptions = { searchSep: '?' }
  ) {
  },
};

var defaultUA = 'Mozilla/5.0 (iPhone; CPU iPhone OS 10_3_1 like Mac OS X) \
AppleWebKit/603.1.30 (KHTML, like Gecko) Version/10.0 Mobile/14E304 Safari/602.1';
