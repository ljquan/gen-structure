import(
    // 正则解析死循环
    /* webpackChunkName: "trans_login" */ '../views/system/trans_login.vue'
)

const originReg = new RegExp(
    '^' + (window?.location?.origin || 'https://www.google.com/').replace(/^https?:/, '(https?:)?') + '/'
);




            {

                fun: (str) => {
                    /['"`]([^'"`\s]+)['"`]/
                },
            },